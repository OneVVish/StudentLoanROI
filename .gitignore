# Secrets -- never commit real credentials. Patterns, not one path: the
# original rule named exactly .streamlit/secrets.toml, so a secrets.toml.bak,
# a copy in another directory, or a .env would have been swept in by the
# `git add -A` workflow this file already warns about twice. The example file
# is re-included because it is documentation and holds only placeholders.
**/secrets.toml
**/secrets.*.toml
!.streamlit/secrets.toml.example
.env
.env.*
*.pem
*.key

# The security review's findings report. Local, like CLAUDE.md: the repo is
# public and the report names what is still open.
SECURITY_REVIEW.md

# Legacy local CSV logs (superseded by Supabase; harmless to ignore either way)
usage_logs.csv
survey_responses.csv

# Python
__pycache__/
*.pyc
.venv/

# Generated by analyze_survey.py -- local research output, not source
analysis_output/

# Research paper draft -- kept local, not part of the app codebase
research_paper_draft.md

# Internal engineering field guide (lessons, constraints, past bugs) --
# deliberately kept out of the public repo, and purged from git history
# 2026-08-06 before the repo went public. Code comments still cite it by
# name; ARCHITECTURE.md is the public map, this is the private manual.
CLAUDE.md

# Slide deck for counselor/teacher outreach -- kept local, not part of the app codebase
presentation/

# Marketing/outreach working files (src taxonomy, email templates, launch copy,
# send logs) -- kept local for the same reason as presentation/
#
# marketing/infographics/ holds the FULL-RESOLUTION chart PNGs (~1.6MB, 2080px
# wide) that the /charts gallery is built from. They stay out of git for the
# reason brand/guide-*.png does: infra/build_site.py writes web-sized JPEGs
# into static/ and those ARE committed, so carrying both would put the same
# picture in the repo twice at ten times the weight. static/ is the archive.
#
# The consequence, which is the whole point of writing it down: a fresh clone
# CANNOT rebuild the gallery images. Three of the six have generator scripts in
# this folder; the other three were hand-patched from exports that have no
# source at all. If static/info-*.jpg is ever deleted, it is gone.
marketing/

# check_reel.py sits at the repo root with the other guards and is the ONE that
# cannot be committed: it imports marketing/reel.py and reel_data.py, and runs
# on marketing/reels/*.md, all of which are inside the ignore above. Committed,
# it would be the first tracked file in this repo that cannot run in a fresh
# clone -- a guard that fails on checkout teaches everyone to skip guards.
#
# The alternative was un-ignoring the reel engine (a !marketing/*.py negation)
# so the guard had its dependencies. That is the better end state and is a
# bigger decision than a gitignore line, because it would also pull in the
# chart scripts, the src taxonomy and the outreach copy this folder exists to
# keep private. Ignored for now, deliberately, and NOT because the guard is
# unimportant: it is what stops a reel stating a stale figure out loud.
check_reel.py

# Agent tooling: skills, settings, worktrees. Local working aids, not part of
# the app -- same reasoning as CLAUDE.md, which is also deliberately untracked.
# Ignored rather than merely untracked because commits here are routinely
# staged with `git add -A`, which would sweep them back in silently.
.claude/

# Everything feeding the research paper stays local, same as the draft above:
# scope/limitations notes, paper structure outlines, and any future drafts.
# Ignored rather than merely untracked -- this repo's commits are routinely
# staged with `git add -A`, which would sweep an untracked file in silently.
SCOPE.md
RETURNING_STUDENT_PLAN.md
*_PLAN.md
paper_outline.md
paper_*.md
*_draft.md
Most-Recent-Cohorts-Institution.csv
# 153 MB raw field-of-study release; build_professional_debt.py reads it and
# the cleaned output (data/graduate_debt_clean.csv) is what ships.
# Download the DATED filename -- the undated one serves an older release, see
# the note in build_professional_debt.py's docstring.
Most-Recent-Cohorts-Field-of-Study.csv
# Raw IPEDS releases for build_graduate_tuition.py; the cleaned output
# (data/graduate_tuition_clean.csv) is what ships. Both the zips as downloaded
# and the CSVs they unpack to.
IC[0-9][0-9][0-9][0-9]_AY.zip
ic[0-9][0-9][0-9][0-9]_ay.csv
HD[0-9][0-9][0-9][0-9].zip
HD[0-9][0-9][0-9][0-9].csv
fos*.zip
national_M2025_dl.xlsx
state_M2025_dl.xlsx
MSA_M2025_dl.xlsx
occupation.xlsx
# CPS ASEC person file for build_hs_age_profile.py -- ~280MB unzipped
pppub*.csv

# Wrangler's local state (build cache, deploy metadata, login token pointer).
# Never contains the token itself -- that lives in ~/.wrangler -- but it is
# per-machine and has no business in the repo.
.wrangler/

# Guide imagery: build output in brand/, SERVED copy in static/. Only the
# served copy is committed -- the pair is byte-identical and 4.7MB, and
# carrying both means every regeneration writes the same bytes to git twice.
#
# Nothing is lost. The two diagrams rebuild from brand/build_guide_graphics.py
# with no credentials, and the AI heroes are not regenerable anyway (a rerun of
# build_ai_hero.py costs an API call and, for a changed prompt, returns a
# different photograph) -- so static/ is the archive, and `--cards-only` reads
# its social cards back out of static/ rather than out of brand/.
#
# The nine OLDER brand/*.png are already tracked and stay that way: an ignore
# rule does not untrack a tracked file, and quietly deleting published assets
# is not what "stop carrying duplicates" asked for.
brand/guide-*.png
# Same rule, same reason, for the calculator link-preview card. Its INPUT is
# the exception and IS committed: brand/sources/ holds a browser screenshot of
# the running app, which no script can regenerate, so it is source rather than
# output.
brand/feature-og-*.png

# Two generated files in static/ that nothing serves, kept on disk but out of
# git so they do not read as published assets:
#   guide-hero-1600x460.png              build_guide_graphics.build()'s
#                                        credential-free fallback hero. Every
#                                        post now names a real hero, so this is
#                                        only proof the repo builds with no
#                                        Cloudflare credentials present.
#   guide-hero-for-parents-run-the-numbers.png
#                                        the pre-AI flat-navy hero for that
#                                        post, superseded by the -klein4b one.
#                                        Committing it would put TWO heroes in
#                                        static/ for one slug, which is exactly
#                                        what broke build_ai_hero --cards-only
#                                        when it globbed instead of reading
#                                        front matter.
static/guide-hero-1600x460.png
static/guide-hero-for-parents-run-the-numbers.png

# macOS Finder droppings; committed once by accident (c183c14).
.DS_Store

# Agent skills installed by `npx skills add` (ScrapeCreators and friends).
# They land INSIDE the repo, and this repo is PUBLIC. They are third-party
# vendor tooling for whoever is working here, not this project's code, so
# committing them would publish someone else's skill definitions and add
# noise nobody reading this repo wants.
#
# skills-lock.json goes with them: it records a personal tooling choice
# rather than a dependency the app has. requirements.txt is where this
# project's real dependencies live, pinned deliberately.
.agents/
skills-lock.json
