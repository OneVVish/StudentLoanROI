# Pinned to exact versions (not >=) deliberately -- an unbounded range let
# Streamlit Cloud's fresh install silently drift to brand-new releases
# (pandas 3.0.3, pyarrow 25.0.0, Streamlit 1.59.1 on Python 3.14.6) that
# were never actually tested against, and one of those combinations
# segfaulted the whole server process on every page load. These exact
# versions are the ones every feature in this app has actually been
# verified against locally. If you deliberately want to move to newer
# versions later, bump one at a time and re-verify, don't drop the pins.
streamlit==1.58.0
pandas==3.0.3
numpy==2.5.0
pyarrow==24.0.0
plotly==6.8.0
requests==2.34.2
st-supabase-connection==2.1.3
openpyxl==3.1.5
reportlab==5.0.0
